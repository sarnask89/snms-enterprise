// ... other imports and types here if any...
function _authEnabled() {
    const val = process.env["AUTH_ENABLED"];
    return !(val === "false" || val == '0' || val == 'no' || val === 'off'); // Returning true only when AUTH is not set to false, no or off in environment variables  
}
function _pathOpen(path) {
    const p = path.split("?", 1)[0]; // Splitting the url by '?' and getting first part after split if it starts with '/static','/teryt/' or any of these paths  
    return !["/login", "logout", "favicon.ico"].includes(p) && (p.startsWith("/client") || (!path_open)); // Returning true only when path is not one mentioned above and starts with '/static','/teryt/' or any of these paths  
}
class PortalUserMiddleware {
    async dispatch(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchBasedOnAuthEnabledAndUserSession(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchBasedOnAuthEnabledAndClientSession(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForStaff(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForClient(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForOthers(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForLegacyRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForAdminPitAndStats(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForNodesAndDiagnostics(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForTertiaryRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForLegacyRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForAdminPitAndStats(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForNodesAndDiagnostics(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForTertiaryRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForLegacyRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForAdminPitAndStats(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForNodesAndDiagnostics(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForTertiaryRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForLegacyRoutes(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForAdminPitAndStats(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForNodesAndDiagnostics(request, callNext) { request.state = {}; } // Initializing state object if it doesn’t exist already       // ... other initializations here...        let path  = this._getPathFromRequest();   return await  _dispatchBasedOnAuthEnabled_andUserSession_(path ,callnext); }
    async *dispatchForTertiaryRoutes(request, callNext) { request.state = {}; }
}
export {};
//# sourceMappingURL=middleware_portal.js.map