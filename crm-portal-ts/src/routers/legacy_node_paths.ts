import { APIRouter } from "fastify"; // Importing the fastapi router module. 
// FastAPI is a web framework for NodeJS, it provides features such like routing and middleware support out of box which makes development easier in terms of setting up routes etc., while Express does not have this feature natively but we can use its functionalities to achieve similar results by using fastify or other frameworks.
import { RedirectResponse } from "fastapi"; // Importing the FastAPI's built-in response module for redirect responses in HTTP requests, which is equivalent of Python’s 'RedirectResponse'. 
// Dependencies: Depends on a function that returns data (like verify_session) to be used as middleware. This way we can ensure certain operations are performed before or after the request/response cycle begins and ends with FastAPI's functionalities, similar like how you would use dependencies in Express routers for routing purposes
import { get }