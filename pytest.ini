[pytest]
pythonpath = .
testpaths = tests
# Remove auto mode to prevent conflicts with sync/playwright tests
# asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: CRUD / sesja jak w przeglądarce (HTTP POST + redirect)
