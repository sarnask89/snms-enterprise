## 2026-05-19 - [Tooltip and Placeholder Patterns]
**Learning:** Nuxt UI v4 `UTooltip` component is used for providing visual labels to icon-only buttons, complementing `aria-label`. Polish placeholders starting with "np." improve form usability by providing clear format examples.
**Action:** Proactively add `UTooltip` and `aria-label` to icon-only buttons and descriptive `placeholder` attributes to form inputs in future tasks.
